@using Flights.Domain.State

<div class="grid mt-3 pr-2">
    <div class="grid grid-cols-7">
        <ShanghaiPoint PointTarget="1" PointValue="@Points?.V1" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="2" PointValue="@Points?.V2" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="3" PointValue="@Points?.V3" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="4" PointValue="@Points?.V4" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="5" PointValue="@Points?.V5" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="6" PointValue="@Points?.V6" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="7" PointValue="@Points?.V7" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="8" PointValue="@Points?.V8" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="9" PointValue="@Points?.V9" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="10" PointValue="@Points?.V10" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="11" PointValue="@Points?.V11" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="12" PointValue="@Points?.V12" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="13" PointValue="@Points?.V13" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="14" PointValue="@Points?.V14" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="15" PointValue="@Points?.V15" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="16" PointValue="@Points?.V16" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="17" PointValue="@Points?.V17" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="18" PointValue="@Points?.V18" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="19" PointValue="@Points?.V19" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="20" PointValue="@Points?.V20" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <ShanghaiPoint PointTarget="25" PointValue="@Points?.Bulls" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
    </div>
</div>

@code{
    [Parameter]
    public ShanghaiState? Points {get;set;}
}    
