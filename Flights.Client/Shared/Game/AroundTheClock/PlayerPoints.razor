@using Flights.Domain.State

<div class="grid pr-2">
    <div class="grid grid-cols-7">
        <AroundTheClockPoint PointTarget="1" PointValue="@Points?.V1" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="2" PointValue="@Points?.V2" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="3" PointValue="@Points?.V3" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="4" PointValue="@Points?.V4" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="5" PointValue="@Points?.V5" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="6" PointValue="@Points?.V6" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="7" PointValue="@Points?.V7" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="8" PointValue="@Points?.V8" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="9" PointValue="@Points?.V9" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="10" PointValue="@Points?.V10" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="11" PointValue="@Points?.V11" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="12" PointValue="@Points?.V12" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="13" PointValue="@Points?.V13" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="14" PointValue="@Points?.V14" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="15" PointValue="@Points?.V15" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="16" PointValue="@Points?.V16" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="17" PointValue="@Points?.V17" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="18" PointValue="@Points?.V18" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="19" PointValue="@Points?.V19" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="20" PointValue="@Points?.V20" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
        <AroundTheClockPoint PointTarget="25" PointValue="@Points?.Bulls" LastTarget="@(Points?.CurrentTarget ?? 0)"/>
    </div>
</div>

@code{
    [Parameter]
    public AroundTheClockState? Points {get;set;}
}    
